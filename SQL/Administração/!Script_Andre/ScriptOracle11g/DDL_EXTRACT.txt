set heading off;
 set echo off;
 Set pages 999;
 set long 90000;


SELECT 'select dbms_metadata.get_ddl('||''''||'TABLE'||''''||','||''''||TABLE_NAME||''''||','||''''||OWNER||''''||') FROM DUAL;'
FROM DBA_TABLES
WHERE OWNER='DBPTN';
