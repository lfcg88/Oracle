select a.sid, a.serial#, b.sql_text
from v$session a, v$sqlarea b
where a.sql_address=b.address
and a.username='FUSION';


select blocking_session,
sid,
serial#,
wait_class,
seconds_in_wait
from v$session
where blocking_session is not NULL
order by blocking_session;