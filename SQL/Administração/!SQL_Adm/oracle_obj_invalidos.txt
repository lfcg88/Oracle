select 'alter '||decode(object_type,'PACKAGE BODY','PACKAGE','TYPE BODY','TYPE','SYNONYM','PUBLIC SYNONYM',object_type)||' '||
decode(owner,'PUBLIC','',owner||'.')||object_name||' '||decode(object_type,'PACKAGE BODY','compile body','TYPE BODY','compile body','compile')||';'
from dba_objects
where status='INVALID'
order by 1;